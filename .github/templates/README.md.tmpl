# subconverter anti-fingerprint edition

这是 [{{UPSTREAM_REPOSITORY}}](https://github.com/{{UPSTREAM_REPOSITORY}}) 的自动同步衍生版本，适合希望减少订阅转换请求特征、同时持续获得上游更新的用户。

本仓库只说明与上游不同的内容。安装、配置、支持格式和 API 用法请查看[上游 README](https://github.com/{{UPSTREAM_REPOSITORY}}#readme)。

## 当前版本

| 项目 | 当前值 |
| --- | --- |
| 上游分支 | `{{UPSTREAM_BRANCH}}` |
| 上游版本 | `{{UPSTREAM_VERSION}}` |
| 上游提交 | [`{{UPSTREAM_COMMIT_SHORT}}`](https://github.com/{{UPSTREAM_REPOSITORY}}/commit/{{UPSTREAM_COMMIT}}) |
| 上游源码同步时间 | `{{UPSTREAM_SYNCED_AT}}` |
| 锁定的 Mihomo Release | [`{{MIHOMO_TAG}}`]({{MIHOMO_RELEASE_URL}}) |
| 锁定的 Mihomo 提交 | [`{{MIHOMO_COMMIT_SHORT}}`](https://github.com/{{MIHOMO_REPOSITORY}}/commit/{{MIHOMO_COMMIT}}) |
| 源码配对 ID | `{{MIHOMO_PAIR_ID}}` |
| 模仿契约 | `{{MIHOMO_PARITY_CONTRACT}}`（helper protocol `{{MIHOMO_HELPER_PROTOCOL}}`） |
| 本项目版本后缀 | `{{EDITION}}` |

## 与上游的区别

- 只有调用链显式标记为 `SubscriptionProvider` 的机场订阅获取路径由配套 helper 执行；helper 从上表锁定的官方 Mihomo tag/commit 源码构建，并复用 Mihomo 的订阅 Provider 获取实现，而不是只替换一个 User-Agent 字符串。
- helper 的 Mihomo 身份、项目适配层和构建输入由 `.github/source-lock.json` 的配对 ID 绑定；身份不匹配时拒绝降级到普通 HTTP 路径。
- 原生构建会把 helper 的精确 SHA-256 固化进 subconverter，并用锁定的官方 Mihomo 可执行文件逐字段对照 HTTP/1.1 请求和 TLS ClientHello；只归一化动态端口、随机数、会话 ID 与临时密钥字节。
- 未标记为 `SubscriptionProvider` 的 Generic 出站请求（包括普通规则和模板下载）继续使用上游的 `subconverter/<version> cURL/<libcurl-version>` User-Agent，不会因为是出站请求就一律伪装成 Mihomo。
- 不发送 `SubConverter-Request`、`SubConverter-Version` 和 CORS `X-Requested-With` 指纹头。
- GET 和 HEAD 请求不再默认发送 JSON `Content-Type`。
- 订阅入口收到的客户端、CDN 和反向代理请求头不会透传到目标地址。
- 脚本化 HTTP 请求显式设置的 User-Agent 和 Content-Type 保持不变。
- Docker 镜像直接从当前仓库提交构建，不会在构建过程中克隆浮动分支。
- 提供带独立版本号的多平台二进制文件，以及 Docker Hub 和 GHCR 多架构镜像。

## 版本标识

程序版本沿用上游格式，并追加本项目标识和本项目提交：

```text
v<上游版本>-<上游提交>-af.<本项目提交>
```

例如：

```text
v0.9.9-633ecd5a-af.1a2b3c4d
```

前半部分用于定位上游版本和提交，`af` 后的提交来自本仓库。Mihomo helper 的身份由源码锁和配对 ID 单独标识，不会混入 subconverter 的程序版本。

## Docker 镜像

```text
docker.io/aethersailor/subconverter:latest
ghcr.io/aethersailor/subconverter:latest
```

除 `latest` 外，每次发布还会提供不可变的完整版本标签和项目提交标签。容器启动后可通过以下接口确认实际运行版本：

```bash
curl http://127.0.0.1:25500/version
```

容器运行方式和配置文件说明与上游一致，请参考[上游文档](https://github.com/{{UPSTREAM_REPOSITORY}}#readme)。

## 自动更新

- 定时检查上游提交和 Mihomo 最新稳定 Release，也支持手动触发。
- Mihomo 更新必须重新解析官方 tag/commit、构建输入与平台基准，并生成新的源码配对 ID；README 不维护第二份 Mihomo 版本号。
- 没有变化时不会创建同步提交或重复发布。
- 候选代码、原生构件和镜像通过测试后，才会更新 `master` 和公开标签。
- 上游原始标签不会被移动，也不会被复制为缺少本项目后缀的发布标签。
