<!--
本文件由 scripts/project_metadata.py 根据
.github/templates/README.md.tmpl、.github/project-metadata.json 和
.github/source-lock.json 自动生成。请修改模板，不要直接编辑 README.md。
-->

# subconverter anti-fingerprint edition

[![Docker Pulls](https://img.shields.io/docker/pulls/aethersailor/subconverter)](https://hub.docker.com/r/aethersailor/subconverter)
[![Docker Build](https://github.com/Aethersailor/subconverter/actions/workflows/docker.yml/badge.svg?branch=master)](https://github.com/Aethersailor/subconverter/actions/workflows/docker.yml)
[![Release](https://img.shields.io/github/v/release/Aethersailor/subconverter)](https://github.com/Aethersailor/subconverter/releases)

本项目旨在最大限度减少原版 subconverter 主动拉取远程订阅时，向订阅服务商暴露的请求特征。在保留原有转换接口和使用方式的基础上，它将 subconverter 访问订阅地址时可被服务商观察到的 HTTP/TLS 等行为，尽可能对齐 Mihomo 内核的 Provider 访问特征，从而尽可能避免被识别为 subconverter 并遭到屏蔽。

它适合仍需由服务端完成传统订阅拉取和转换、又希望降低请求特征暴露的用户。已经在使用普通 subconverter 的部署通常只需替换镜像，无需改变订阅链接和客户端用法。

> [!TIP]
> 如果希望从架构上完全避免**订阅转换后端访问远程订阅服务商**，建议使用 [Aethersailor/SubConverter-Extended](https://github.com/Aethersailor/SubConverter-Extended)。它会生成 `proxy-provider`，由用户客户端中的 Mihomo 内核直接拉取订阅，转换后端不再连接远程订阅服务器。

## 快速部署

### Docker Compose（推荐）

新建 `compose.yaml`：

```yaml
services:
  subconverter:
    image: docker.io/aethersailor/subconverter:latest
    container_name: subconverter
    restart: unless-stopped
    ports:
      - "127.0.0.1:25500:25500"
```

启动并检查版本：

```bash
docker compose up -d
curl --fail http://127.0.0.1:25500/version
```

也可以使用 GHCR 镜像：

```text
ghcr.io/aethersailor/subconverter:latest
```

### Docker 命令

```bash
docker run -d \
  --name subconverter \
  --restart unless-stopped \
  -p 127.0.0.1:25500:25500 \
  docker.io/aethersailor/subconverter:latest
```

示例默认只允许本机访问，适合搭配同机的 Nginx、Caddy 或 OpenResty。需要从局域网直接访问时，可把端口映射改为 `25500:25500`；不要在没有访问控制的情况下直接暴露到公网。

## 开始使用

转换接口与普通 subconverter 相同：

```text
http://127.0.0.1:25500/sub?target=<目标格式>&url=<URL 编码后的机场订阅地址>
```

例如，把一个示例订阅转换为 Clash 配置：

```bash
curl --get 'http://127.0.0.1:25500/sub' \
  --data-urlencode 'target=clash' \
  --data-urlencode 'url=https://example.com/subscribe?token=REPLACE_ME' \
  --output config.yaml
```

上面的 `curl` 适合测试；日常使用时，可以把完整的 `/sub?...` 地址直接添加为客户端订阅。

常用目标格式：

| 客户端或格式 | `target` |
| --- | --- |
| Clash / Mihomo | `clash` |
| sing-box | `singbox` |
| Surge | `surge`，并通过 `ver=2` 至 `ver=5` 选择版本 |
| Quantumult X | `quanx` |
| Loon | `loon` |
| V2Ray | `v2ray` |
| Shadowsocks / SSR | `ss` / `ssr` |

需要合并多个订阅时，先用 `|` 连接多个地址，再把整个 `url` 参数进行 URL 编码。高级参数、外部配置和完整格式列表请查看[上游使用文档](https://github.com/{{UPSTREAM_REPOSITORY}}#readme)。

## 这版解决什么问题

一次转换包含两段不同的通信：

```text
你的客户端  →  subconverter  →  机场订阅服务商
              正常转换服务       模拟 Mihomo Provider 获取行为
```

本项目只优化右侧这段“subconverter 获取机场原始订阅”的行为：

- 使用当前锁定的 Mihomo 最新稳定版 Provider 获取实现，而不是简单修改 User-Agent；
- 对齐可观测的 HTTP 请求、请求头顺序、TLS ClientHello、重定向、ETag 和代理访问行为；
- 不把客户端、CDN 或反向代理传来的请求头继续转发给机场；
- 身份或组件校验失败时直接让该次获取失败，不会悄悄退回普通 cURL 请求。

订阅转换功能、`/sub` 接口、规则和模板下载方式仍与上游保持一致。

## 它不能隐藏什么

本项目减少的是应用层请求特征，不承诺让访问“绝对不可识别”。机场仍可能观察到：

- 服务器出口 IP 和网络路径；
- 请求时间、频率以及订阅内容；
- 账号本身的使用模式；
- 外层 CDN、WAF、反向代理和访问日志留下的信息。

如果你通过反向代理提供服务，还应阅读[隐私部署说明](docs/PRIVACY.md)，避免完整订阅 URL 和 Token 被写入日志。

## 更新

Compose 部署可直接执行：

```bash
docker compose pull
docker compose up -d
curl --fail http://127.0.0.1:25500/version
```

仓库会自动检查 subconverter 上游源码和 Mihomo 最新稳定 Release。只有源码、原生程序和多架构镜像全部通过测试后，才会更新公开的 `latest` 镜像；预发布版不会被当作最新稳定版自动采用。

若希望固定版本，请从 [Releases](https://github.com/Aethersailor/subconverter/releases) 选择版本标签，或使用镜像 digest，而不是跟随 `latest`。

## 下载与平台

- Docker Hub：`docker.io/aethersailor/subconverter:latest`
- GitHub Container Registry：`ghcr.io/aethersailor/subconverter:latest`
- 原生程序：[GitHub Releases](https://github.com/Aethersailor/subconverter/releases)
- Docker 架构：`linux/amd64`、`linux/386`、`linux/arm/v7`、`linux/arm64`
- 原生程序：Linux、Windows、macOS

## 常见问题

### 它会把所有请求都伪装成 Mihomo 吗？

不会。只有访问机场原始订阅的链路使用 Mihomo Provider 行为。规则、模板等普通下载仍保持 subconverter 自己的身份，避免改变不相关功能。

### 它是否始终跟随 Mihomo 最新版本？

自动化会检查 Mihomo 最新稳定 Release，但不会在未经验证时直接更新。通过源码身份、协议行为、原生构建和镜像测试后才会发布，因此公开镜像对应的是“最新且已经通过本项目验证”的稳定版本。

### 可以保证机场百分之百无法识别吗？

不能。它尽可能缩小 Mihomo 与 subconverter 在 Provider 获取请求上的可观测差异，但无法隐藏出口 IP、访问时机、账号行为或机场服务端掌握的其他数据。

### 能直接替换原来的 subconverter 吗？

可以。接口、端口和主要配置方式保持上游兼容。使用自定义 `pref.ini`、外部配置或规则文件的部署，只需继续挂载原来的配置，并把镜像地址换成本项目镜像。

### 获取订阅失败时会退回普通请求吗？

不会。内置的 Mihomo 请求组件、平台或身份校验失败时会失败关闭，以免在用户不知情的情况下暴露普通 subconverter 请求特征。

## 当前版本

| 组件 | 当前构建基线 |
| --- | --- |
| subconverter 上游 | `{{UPSTREAM_VERSION}}` · [`{{UPSTREAM_COMMIT_SHORT}}`](https://github.com/{{UPSTREAM_REPOSITORY}}/commit/{{UPSTREAM_COMMIT}}) |
| Mihomo 稳定版 | [`{{MIHOMO_TAG}}`]({{MIHOMO_RELEASE_URL}}) · [`{{MIHOMO_COMMIT_SHORT}}`](https://github.com/{{MIHOMO_REPOSITORY}}/commit/{{MIHOMO_COMMIT}}) |
| 上游同步时间 | `{{UPSTREAM_SYNCED_AT}}` |

<details>
<summary>开发与审计信息</summary>

内部只有显式标记为 `SubscriptionProvider` 的机场订阅获取请求会交给 companion helper；Generic 出站请求仍走上游 cURL 链路。它不是只替换一个 User-Agent 字符串，而是从锁定的 Mihomo 源码构建 helper，并复用 Mihomo 的 Provider 获取实现。

| 身份项 | 当前值 |
| --- | --- |
| 上游仓库与分支 | [`{{UPSTREAM_REPOSITORY}}`](https://github.com/{{UPSTREAM_REPOSITORY}}) / `{{UPSTREAM_BRANCH}}` |
| 上游完整提交 | `{{UPSTREAM_COMMIT}}` |
| Mihomo 完整提交 | `{{MIHOMO_COMMIT}}` |
| 源码配对 ID | `{{MIHOMO_PAIR_ID}}` |
| 模仿契约 | `{{MIHOMO_PARITY_CONTRACT}}`（helper protocol `{{MIHOMO_HELPER_PROTOCOL}}`） |
| 项目版本后缀 | `{{EDITION}}` |

进一步资料：

- [helper IPC 与安全协议](mihomo-fetcher/PROTOCOL.md)
- [锁定构建与多平台打包契约](scripts/MIHOMO_FETCHER_PACKAGING.md)
- [一致性捕获和差异判定](scripts/mihomo_conformance/README.md)

运行时可以核对容器中的 helper 和 manifest：

```bash
docker exec subconverter sha256sum /usr/bin/subconverter-mihomo-fetcher
docker exec subconverter cat /usr/share/subconverter/subconverter-mihomo-fetcher.manifest.json
```

</details>

## 相关项目

- subconverter 上游：[{{UPSTREAM_REPOSITORY}}](https://github.com/{{UPSTREAM_REPOSITORY}})
- Mihomo：[MetaCubeX/mihomo](https://github.com/MetaCubeX/mihomo)
