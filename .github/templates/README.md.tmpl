# subconverter anti-fingerprint edition

本仓库定时同步 [{{UPSTREAM_REPOSITORY}}](https://github.com/{{UPSTREAM_REPOSITORY}})，并自动应用本项目维护的反指纹修改。

使用方法、配置说明、支持格式和 API 文档请直接查看[上游 README](https://github.com/{{UPSTREAM_REPOSITORY}}#readme)。

## 当前状态

| 项目 | 当前值 |
| --- | --- |
| 上游分支 | `{{UPSTREAM_BRANCH}}` |
| 上游版本 | `{{UPSTREAM_VERSION}}` |
| 上游提交 | [`{{UPSTREAM_COMMIT_SHORT}}`](https://github.com/{{UPSTREAM_REPOSITORY}}/commit/{{UPSTREAM_COMMIT}}) |
| 上次成功同步 | `{{UPSTREAM_SYNCED_AT}}` |
| 当前出站 User-Agent | `{{USER_AGENT}}` |
| User-Agent 来源 | [{{USER_AGENT_SOURCE}}](https://github.com/{{USER_AGENT_SOURCE}}/releases/latest) |
| 本项目版本后缀 | `{{EDITION}}` |

## 本项目修改

- 出站订阅和规则请求默认使用自动跟踪的当前 User-Agent。
- 移除出站请求中的 `SubConverter-Request`、`SubConverter-Version` 和 CORS `X-Requested-With` 指纹头。
- GET 和 HEAD 请求不再默认发送 JSON `Content-Type`。
- 订阅入口的客户端 UA、Host 和代理链指纹头不会透传到目标地址。
- 脚本化 HTTP 请求显式设置的 User-Agent 和 Content-Type 保持不变。
- Docker 镜像直接从当前检出的提交构建，不在构建过程中克隆浮动分支。
- 发布带独立版本号的多平台二进制文件，以及 Docker Hub 和 GHCR 多架构镜像。

## 版本格式

本项目保留上游动态版本格式，并在其后追加本项目标识和本项目提交：

```text
v<上游版本>-<上游提交>-af.<本项目提交>
```

示例：

```text
v0.9.9-633ecd5a-af.1a2b3c4d
```

User-Agent 版本不会进入程序版本、Git 标签、Release 名称、构件名称或 Docker 标签。

## 镜像

```text
docker.io/aethersailor/subconverter:latest
ghcr.io/aethersailor/subconverter:latest
```

除 `latest` 外，还会发布不可变的完整版本标签和项目提交标签。可通过以下接口查看运行版本：

```bash
curl http://127.0.0.1:25500/version
```

## 自动化

- 每日检查上游提交和 User-Agent，也支持手动触发。
- 无变化时不创建提交，也不重复发布。
- 候选代码、原生构件和镜像测试通过后，才推进 `master` 和公开标签。
- 不移动或复用无本项目后缀的上游标签。
